pyinstaller --onefile --windowed --name "ZST Excel Combiner" ZST-to-XLS.py

to create an .exe


Decodes multiple or single proprietary ZST instrument files (.ZST) and combines them into a single .xlsx file.
